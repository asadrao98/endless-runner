{
  "name": "endless-runner",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "A minimal Three.js endless runner.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host",
    "deploy": "vite build && firebase deploy --only hosting"
  },
  "dependencies": {
    "firebase": "^10.14.1",
    "three": "^0.160.1"
  },
  "devDependencies": {
    "firebase-tools": "^13.29.1",
    "vite": "^5.4.10"
  }
}
